var CLICK_Cvar CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable

var CLICK_C

var CLICK_C

var CLICK_Cvar CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_C
var CLICK_Cvar CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable

var CLICK_Cvar CLICK_Cvar CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_C
var CLICK_Cvar CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_C
var CLvar CLICK_COUNT = 1000; // Renamed variable
var CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_C
var CLICK_Cvar CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_C
var CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable

var CLICK_C
var CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable

var CLvar CLICK_COUNT = 1000; // Renamed variable

var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_C
var CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable

var CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable

var CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable

var CLICK_var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variablevar CLICK
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_C
var CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable

var CLICK_var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variablevar CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_C

var CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_C
var CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable

var CLICK_var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variablevar CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_C

var CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variablevar CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_C
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variableCvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_C

var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rectvvar Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable


var CLICK_COUNT = 1000; // Renamed variable
    const rectvvar Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_C
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rectvvar Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_C
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variableLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rectvvar CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variablevar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_Cvar CLvar CLICK_COUNT = 1000; // Renamed variable
var CLICK_C
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rectvvar CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rectvvar CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = eleonst rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variableOUNT = 1000; // Renamed vaICK_COUNT = 1000; // Renamed variable
var CLICK_C
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rectvvar CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = eleonst rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variableOUNT = 1000; // Renamed variable
    const rectvvar CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_C
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rectvvar CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = eleonst rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variableOUNT = 1000; // Renamed variable
    const rectvvar CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = eleonst rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();ment.getBoundingClientRect();var CLICK_COUNT = 1000; // Renamed variable = eleonst rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variableriable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();ment.getBoundingClientRect();var CLICK_COUNT = 1000; // Renamed variable = eleonst rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();ment.getBoundingClientRect();var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; /var CLICK_COUNT = 1000; // Renamed variable
    const rect = eleonst rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();ment.getBoundingClientRect();var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable

var CLICK_COUNT = 1000; // Renamed variable
    const rect = eleonst rect = element.getBoundingClientRect();ar CLICK_COUNT = 1000; / Renamed variable

var CLICK_COUNT = 1000; // Renamed variable
    const rect = eleonst rect = element.getBoundingClientRect();ar CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = eleonst rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();ment.getBoundingClientRect();var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = eleonst rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();ment.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable

    const rect = element.getBvar CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable

    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variableoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
   const rect = element.getBoundingClientRect();var CLICK_COUNT = 1000; // Re    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable

    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
   const rect = element.getBoundingClientRect();var CLICK_COUNT = 1000; // Re    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable

    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
   const rect = element.getBoundingClientRect();var CLICK_COUNT = 1000; // Re    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable

    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
   const rect = element.getBoundingClientRect();var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
   const rect = element.getBoundingClientRect();var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable

var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
   const rect = element.getBoundingClientRect();var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable   const rect = element.getBoundingClientRect();var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable

var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
   const rect = element.getBoundingClientRect();var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
   const rect = element.getBoundingClientRect();var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable

    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable

    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
    const rect = element.getBoundingClientRect();
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();

var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();

var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable

    const rect = element.getBoundingClientRect();

var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable

    const rect = element.getBoundingClientRect();
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();

var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();

var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();

 const rect = element.getBoundingClientRect();
    if (!element) {
var CLICK_COUNT = 1000; // Renamed variable
    const rect = element.getBoundingClientRect();

 const rect = element.getBoundingClientRect();
    if (!element) {
        console.error('Element not found!');
        return;
    }

    const rect = element.getBoundingClientRect();
(async function clickLoop(n) {
    const element = document.querySelector('[class*="mt-[10px]"]');
    if (!element) {
        console.error('Element not found!');
        return;
    }
        return;
    }

    const rect = element.getBoundingClientRect();
(async function clickLoop(n) {
    const element = document.querySelector('[class*="mt-[10px]"]');
    if (!element) {
        console.error('Element not found!');
        return;
    const rect = element.getBoundingClientRect();

    for (let i = 0; i < n; i++) {
        const x = Math.random() * rect.width + rect.left;
        const y = Math.random() * rect.height + rect.top;

        ['pointerdown', 'mousedown', 'mouseup', 'click'].forEach(eventType => {
            const event = new MouseEvent(eventType, {
                bubbles: true,
                cancelable: true,
                clientX: x,
                clientY: y
            });
            element.dispatchEvent(event);
        });

        console.log(`Click ${i + 1}/${n} at: (${x.toFixed(2)}, ${y.toFixed(2)})`);

        const delay = Math.random() * (300 - 100) + 100;
        await new Promise(resolve => setTimeout(resolve, delay));
    }
})(CLICK_COUNT);





































































































































